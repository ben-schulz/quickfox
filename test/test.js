var assert = require('assert');
describe('what is truth', function() {
  it('is absrud', function() {
    assert.equal(true, false);
  });

  it('is real', function() {
    assert.equal(true, true);
  });
});
